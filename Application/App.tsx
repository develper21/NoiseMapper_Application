import 'expo-router/entry';
import './global.css';

// For traditional Expo setup (when not using Expo Router)
export default function App() {
  return null; // This won't be used when using Expo Router
}
